import StudentApp from "@/components/StudentApp";

export const metadata = {
  title: "Bettr — Your Feed",
  description: "Bettr: algorithms, verification, rewards. Your main app.",
};

export default function MainAppPage() {
  return <StudentApp />;
}
